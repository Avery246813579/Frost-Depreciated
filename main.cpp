#include<iostream>
#include<fstream>
#include <algorithm>
#include "Test.h"
#include <typeinfo>
#include "Character.h"
#include "Integer.h"

using namespace std;

int main() {
//	string line, bulk = "";
//	ifstream myfile ("file.frost");
//	if (myfile.is_open()) {
//		while ( getline (myfile,line) ) {
//			cout << line << '\n';
//			bulk += line;
//		}
//		myfile.close();
//	}
//
//	size_t n = std::count(bulk.begin(), bulk.end(), '{');

    Integer i("3sd1");
	Character cr("3");

	return 0;
}
